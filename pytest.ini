[pytest]
testpaths = tests
norecursedirs = .git .claude output* *.egg-info build dist src/*/src _libs
markers =
    slow: marks tests as slow to run
    stats: statistical / numerical-methodology tests
    integration: integration tests spanning multiple modules
